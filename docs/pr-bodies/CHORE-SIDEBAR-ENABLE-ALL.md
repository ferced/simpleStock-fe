### Qué se hizo

- Se habilitaron todas las entradas del sidebar en `src/config/navigation.ts` (prop `enabled: true`).
- Incluye hijos (p. ej. `Productos > Categorías`).

### Motivo

- Facilitar demo y navegación completa. Se puede volver a ocultar cambiando `enabled` a `false`.

### Archivos

- `src/config/navigation.ts`



